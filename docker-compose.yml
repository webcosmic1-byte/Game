version: '3.8'

services:
  arcade-app:
    build: .
    container_name: ai-arcade-engine
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html/
    environment:
      - ADMIN_PIN=7785
    restart: always
